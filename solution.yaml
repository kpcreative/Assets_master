apiVersion: solution.sap/v1
kind: Solution

metadata:
  name: asset-master-cleanup-f02a1
  version: 1.0.0
  description: >
    Asset Master Data Monitor & Cleanup Platform — monitors S/4HANA fixed
    asset master data for stale/incomplete records, alerts users, and enables
    a governed two-step review/approval workflow with automatic write-back
    to S/4HANA.

assets:
  - ref: ./assets/asset-master-cleanup-cap/asset.yaml
